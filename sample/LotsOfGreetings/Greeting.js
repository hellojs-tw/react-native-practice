import React, { Component } from "react";
import { Text, View } from "react-native";
// import prop-types node module

export default class Greeting extends Component {
  // set default props

  // set props type validation

  render() {
    // return greeting text
    return (
      <View />
    );
  }
}